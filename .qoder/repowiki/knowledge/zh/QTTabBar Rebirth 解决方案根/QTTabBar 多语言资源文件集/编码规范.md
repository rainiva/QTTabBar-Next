- 每种语言一个独立文件，文件名遵循 `Lng_QTTabBar_<语言代码>.xml` 命名规范。
- XML 根节点为 `<root>`，内部按功能域划分同名节点，节点内每行一条字符串，按顺序与宿主硬编码索引一一对应。
- 元信息区使用 `<Author>`、`<Language>`、`<Country>`、`<Version_QTTabBar>`、`<DateModified>` 等固定节点记录翻译作者与版本。