基于 .NET Framework 的 COM 互操作（`QTPlugin.Interop.IShellBrowser`、`IDLWrapper`、`PInvoke.ILCreateFromPath`）与 Windows Shell 集成；插件签名校验使用 `X509Certificate2` + `X509Chain`（Authenticode，离线吊销列表）以及强名称 `PublicKeyToken` 比对。