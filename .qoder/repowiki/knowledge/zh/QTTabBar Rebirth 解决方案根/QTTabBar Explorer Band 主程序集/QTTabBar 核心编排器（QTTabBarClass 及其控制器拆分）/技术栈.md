C# WinForms + COM Interop，通过 `BandObjectLib.BandObject` 基类加载为 Explorer Shell Band；使用 `SHDocVw.WebBrowser` 与 `Interop.IShellFolder/IShellView` 与 Explorer 通信；自定义窗口消息通过 `PInvoke.RegisterWindowMessage("QTTabBar_*")` 注册。