- Windows Shell COM 接口统一放在 Interop/ 下以纯 C# 声明，Common/ 中再提供托管包装类，避免在业务代码中直接写 P/Invoke
- 大型类按功能拆分到同名分部文件（如 QTTabBarClass.*.cs），每个分部聚焦一个职责域（Band 生命周期、输入钩子、菜单等）
- 全局状态通过顶层静态类（Config/SessionState/IconManager 等）暴露，子模块以静态方式访问而非构造函数注入