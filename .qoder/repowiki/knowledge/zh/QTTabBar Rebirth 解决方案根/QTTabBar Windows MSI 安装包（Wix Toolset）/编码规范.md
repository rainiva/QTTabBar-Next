- 每个文件/组件用独立 GUID 标识，且以 `27af1a85-9a0x-4c79-bdeb-d73fd4bd957x` 系列递增分配，避免重复。
- 对 x86/x64 双架构统一采用 `<?foreach arch in x86;x64 ?>` 配合 `$(var.win64)` 条件变量生成 Component/RegistryKey，并在 x64 分支附加 `Condition VersionNT64`。
- COM 对象通过 `<Class ForeignServer="mscoree.dll">` + `HKCR\CLSID\{GUID}\InprocServer32` 下写入 Class/Assembly/RuntimeVersion 三值的方式注册，而非直接指向 .dll。
- 所有 RegistryKey 均设置 `ForceCreateOnInstall="yes" ForceDeleteOnUninstall="yes"`，确保卸载时清理。
- 语言资源统一放在 `lang*.wxl` 文件中，通过 `!(loc.xxx)` 语法引用，默认语言为 en-us (1033)。