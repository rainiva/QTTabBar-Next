- plans 与 specs 文件采用 ISO 日期前缀命名（如 `2026-07-06-xxx.md`），同一功能在 plans/ 与 specs/ 下各有一份同名文件，形成一对可追踪的工件。
- plans 使用 `- [ ]` 复选框语法拆分任务步骤，并在每个 Step 后给出显式的 Run / Expected 命令块，使 agentic worker 可直接执行验证。
- 所有设计文档顶部以 YAML-like 元数据块声明版本、日期、目标范围或状态，便于快速扫描变更上下文。