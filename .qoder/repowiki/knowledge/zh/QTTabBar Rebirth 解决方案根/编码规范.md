- 插件工程统一位于 `Plugins/<Name>/<Name>.csproj` 并按 `NestedProjects` 归入 `Plugins` 分组节点。
- 构建变体通过方案级 `GlobalSection(SolutionConfigurationPlatforms)` 集中定义，各子项目再映射到具体 ActiveCfg。
- C++ 原生库（MinHook、QTHookLib、InstallerHelper）与 C# 托管代码混合编译，通过 `ProjectDependencies` 声明跨语言依赖顺序。