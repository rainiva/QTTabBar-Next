.NET Framework 4.8 (v4.8) + WinForms；通过 `[ComImport]` 直接声明 Shell COM 接口，并通过 `Interop.SHDocVw` 互操作程序集访问 `SHDocVw.WebBrowser`；DPI 感知使用 Shcore `GetDpiForMonitor`/`GetDpiForWindow` 与 user32 `MonitorFromPoint/MonitorFromWindow`。