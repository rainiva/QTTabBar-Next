WCF 双工管道 (`NetNamedPipeBinding`, `SessionMode.Required`) 承载跨 Explorer 实例 IPC；委托序列化采用 `SerializationHelper.ObjectToByteArray(new SerializeDelegate(...))`（BinaryFormatter 包装），传输配额统一为 `MaxIpcMessageBytes = 4MB`；配置项默认值根据 `QTUtility.IsThanWin11`/`IsWin7`/`IsXP` 分支初始化。