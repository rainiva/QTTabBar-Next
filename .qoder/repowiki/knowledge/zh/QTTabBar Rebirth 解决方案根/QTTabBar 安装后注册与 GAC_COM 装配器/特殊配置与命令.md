在普通命令行中直接双击 `Register/Register.bat [Configuration]` 即可触发注册；或在 Developer Command Prompt 中执行 `msbuild Register/Register.vcxproj /p:QtTabBarRegisterMode=Apply` 以在每次构建后自动完成 GAC 与 COM 注册。设置环境变量 `QT_TABBAR_REGISTER_VALIDATE_ONLY=1` 可仅校验环境而不实际注册。