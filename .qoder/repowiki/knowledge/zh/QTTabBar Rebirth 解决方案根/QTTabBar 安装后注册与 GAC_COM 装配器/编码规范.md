- 所有注册操作均先检查目标 DLL 是否存在（`IF EXIST *.dll`）再调用 gacutil/regasm，避免缺失产物时中断整个流程。
- 环境探测采用分层回退策略：当前 shell → vswhere+VsDevCmd/vcvarsall → VS2010 VCVarsQueryRegistry，每层失败即继续下一层，最终通过 `exit /b 1` 统一报告失败。
- 32/64 位对称处理：对 regasm 分别查找 `FrameworkDir32/64` 下的版本，对注册表写入使用 `/reg:32` 与 `/reg:64` 双视图同步写入 `InstallPath`。