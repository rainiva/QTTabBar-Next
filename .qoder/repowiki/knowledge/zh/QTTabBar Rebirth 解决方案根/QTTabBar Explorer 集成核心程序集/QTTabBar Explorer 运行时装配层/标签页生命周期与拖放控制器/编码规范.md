- 每个控制器都是 `public partial class QTTabBarClass` 下的 `internal class`，并通过私有只读字段 `readonly QTTabBarClass _owner` 持有宿主引用，所有外部操作一律经 `_owner.xxx` 转发。
- 文件头部注释标明来源批次（如 `Task 27 / Batch 12 extracted from QTTabBarClass`、`arch-batch3a`、`arch-batch3c6s`），强调这是从主类拆出的结构体迁移，行为不得变更。
- 对 `TabControl` 的批量修改统一包裹在 `_owner.tabControl1.SetRedraw(false)` / `SetRedraw(true)` try-finally 中以抑制闪烁。
- 路径/IDL 解析一律通过 `using (IDLWrapper wrapper = new IDLWrapper(path)) { if (!wrapper.Available) continue; ... }` 模式进行可用性检查后再消费。
- 鼠标/键盘组合动作通过 `QTUtility.MakeMouseChord(MouseChord.Xxx, Control.ModifierKeys)` 生成键，再查 `Config.Mouse.TabActions` / `BarActions` 映射到 `BindAction`，最后由 `_owner.DoBindAction(action, false, tab)` 派发。
- 需要刷新按钮栏的地方统一调用 `TryCallButtonBar(bbar => bbar.RefreshButtons())`，避免直接耦合 ButtonBar。