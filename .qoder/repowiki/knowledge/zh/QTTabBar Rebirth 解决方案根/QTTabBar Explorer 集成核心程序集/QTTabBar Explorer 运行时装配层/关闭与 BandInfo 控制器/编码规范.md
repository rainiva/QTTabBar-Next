- 拆出的控制器统一声明为 `internal class`，并通过 `readonly QTTabBarClass _owner` 构造注入，避免直接访问外部实例。
- 主类对控制器的调用采用“单行转发”模式：`public override void CloseDW(...) => _shutdownController.CloseDW(...)`，不在主类内保留业务细节。
- COM 资源释放遵循“判空 → Dispose/ReleaseHandle/FinalReleaseComObject → 置 null”的顺序，并以 try/catch 包裹后记录到 `QTUtility2.MakeErrorLog`。
- DES KBANDINFO 更新按 `dbi.dwMask & DBIM.XXX != 0` 分支逐个设置对应字段，未覆盖的位保持 Explorer 默认值。